openapi: 3.0.0
info:
  title: Jobs API
  version: 1.0.0
servers:
  - url: https://zero6-jobs-api-ac4z.onrender.com/api/v1
paths:
  /auth/register:
    post:
      summary: Register User
      responses:
        '201':
          description: Created
        '400':
          description: Bad Request
  /jobs:
    get:
      summary: Get all jobs
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT